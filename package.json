{
  "name": "upala-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ upala-test",
    "create-local": "graph create --node http://localhost:8020/ upala-test",
    "remove-local": "graph remove --node http://localhost:8020/ upala-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 upala-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.27.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
